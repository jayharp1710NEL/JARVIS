# JARVIS-LOCAL backend — core dependencies (all local, no cloud required).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.1
httpx>=0.26
sqlalchemy>=2.0
typer>=0.9
rich>=13.0
beautifulsoup4>=4.12
numpy>=1.24
python-multipart>=0.0.9

# ---- Optional capabilities (install as needed) ----
# File RAG (PDF / DOCX):
pypdf>=4.0
python-docx>=1.1
# High quality web article extraction:
trafilatura>=1.6
# Real semantic embeddings (otherwise a local hashing embedder is used):
# sentence-transformers>=2.5
# Alternative vector DB:
# chromadb>=0.4
# Dynamic page rendering:
# playwright>=1.40

# ---- Dev ----
pytest>=8.0
pytest-asyncio>=0.23
